/**
 * Used to identify possible sections in the navigable content container. (The one with the sticky nav)
 */
export enum ContentSectionTypes {
    productDescription,
    productDocuments,
    productSpecs,
    relatedProducts,
    richText,
    imageCardSectionGroup,
    contentCardSection,
}
