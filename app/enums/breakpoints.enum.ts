/**
 * Enum used to determine breakpoints in angular code.  Should match css breakpoints
 */

export enum Breakpoints {
    medium = '(min-width: 640px)',
    mediumHomepage = '(min-width: 800px)',
    large = '(min-width: 1024px)',
}
