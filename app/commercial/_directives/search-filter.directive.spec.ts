import { SearchFilterDirective } from './search-filter.directive';

describe('SearchFilterDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchFilterDirective();
    expect(directive).toBeTruthy();
  });
});
